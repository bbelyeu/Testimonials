CakePhp 2.x Plugin for storing & displaying testimonial info for a business website

Setup:
1. Import testimonials.sql in Config/Schema/

2. Add the following line to your app/Config/bootstrap.php
    CakePlugin::load('Testimonials', array('routes' => true));

3. Use /admin/testimonials to manage
